extends layout

block content
  section.band.page
    .stack
      p.eyebrow= code
      h1= heading
      p.lede= message
      .button-row
        a.button(href="/portfolio") View available properties
        a.button.button-quiet(href="/") Return to the home page
