extends layout

block content
  section.band.page.page-narrow
    h1= heading
    p.lede= lede
    each paragraph in body
      p= paragraph
    .button-row
      a.button(href=action.href)= action.label
